// If you're not using TypeScript, you can use JSDoc for type definitions
/**
 * @typedef {Object} Item
 * @property {string} id
 * @property {string} name
 * @property {number} value
 */

/**
 * @typedef {Object} Player
 * @property {string} id
 * @property {string} name
 * @property {number} level
 * @property {number} experience
 * @property {Item[]} inventory
 */

// Export these if you're using a module system that supports it
module.exports = {};
