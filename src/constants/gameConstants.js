// Constants shared between client and server
const MAX_PLAYER_LEVEL = 100;
const MAX_INVENTORY_SIZE = 50;
const TRADE_TIMEOUT_MINUTES = 30;

module.exports = {
  MAX_PLAYER_LEVEL,
  MAX_INVENTORY_SIZE,
  TRADE_TIMEOUT_MINUTES
};
